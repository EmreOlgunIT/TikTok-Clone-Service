package com.example.MyProject.user;

public enum Role { //An enum is a special "class" that represents a group of constants. Enum is short for "enumerations", which means "specifically listed". Enum constants are public, static and final (unchangeable - cannot be overridden).
    USER,
    ADMIN
}
